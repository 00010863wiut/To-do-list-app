extends layout 

block content 
    .containerAllTasks
        h1 All tasks 

        each task in tasks 
            .task_wrapper
                .taskTitle
                    input.checkbox(type="checkbox" value="1" name="checkbox")
                    a.task(href='/tasks/' + task.id)= task.title
                .buttons
                    a(href="/tasks/" + task.id + "/delete").delete Delete
                    a.finished(href='#') Finished
    .circle 
    .circleSecond
    .circleThird
