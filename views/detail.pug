extends layout 

block content 
    .circle.detail
    .circle.detailone

    .detail
        .containerDetail
            h1.title #{ task.title }
            h3.datetime Due #{ task.datetime }
            p.description #{ task.description }
